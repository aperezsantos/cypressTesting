// passing arguments into a function the regular JS way
forecast(80209, 7, 'fahrenheit', true);

// passing props (arguments) into a component (function) the React way
<Forecast zip={80209} days={7} units="fahrenheit" detailed />;
